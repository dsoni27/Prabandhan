<UserControl x:Class="Parbandhan.Views.AddAssetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:local="clr-namespace:Parbandhan.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0"
                        Orientation="Horizontal" 
                        VerticalAlignment="Center" 
                        Margin="0,0,0,0">
                <TextBlock Text="Asset Type" FontSize="20" FontWeight="Regular" Foreground="{StaticResource color12}" Margin="10,0,0,0"/>
                <ComboBox x:Name="cmbAssetType"
                          ItemsSource="{Binding AssetTypes}"
                          DisplayMemberPath="AtName"
                          SelectedValuePath="Atid"
                          Width="200"  Margin="20,0,0,0" VerticalAlignment="Center"
                          SelectedValue="{Binding SelectedAssetTypeId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"/>
                <Label Content="{Binding ElementName=cmbAssetType, Path=(Validation.Errors)[0].ErrorContent}" 
                       Foreground="Red" FontSize="12" Margin="0,2,0,0"/>
                <TextBlock Foreground="Red" FontSize="12" Margin="5,2,0,0"
                           Text="{Binding (Validation.Errors)[0].ErrorContent,
                                          RelativeSource={RelativeSource PreviousData}}"/>
            </StackPanel>
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal" 
                        VerticalAlignment="Center" 
                        Margin="0,0,0,0">
                <TextBlock Text="Source" FontSize="20" FontWeight="Regular" Foreground="{StaticResource color12}" Margin="10,0,0,0"/>
                <ComboBox x:Name="cmdStorageLocation"
                          ItemsSource="{Binding StorageLocations}"
                          DisplayMemberPath="SlName"
                          SelectedValuePath="Slid"
                          Width="200"  Margin="20,0,0,0" VerticalAlignment="Center"
                          SelectedValue="{Binding SelectedStorageLocationId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"/>
                <Label Content="{Binding ElementName=cmdStorageLocation, Path=(Validation.Errors)[0].ErrorContent}" 
                       Foreground="Red" FontSize="12" Margin="0,2,0,0"/>
                <TextBlock Foreground="Red" FontSize="12" Margin="5,2,0,0"
                           Text="{Binding (Validation.Errors)[0].ErrorContent,
                                    RelativeSource={RelativeSource PreviousData}}"/>
            </StackPanel>
            <Button Grid.Column="2"
                    Width="100" Height="30">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <fa:IconImage Icon="Search" Margin="0,0,8,0"/>
                    <TextBlock Text="Search" 
                               VerticalAlignment="Center"
                               FontFamily="Trebuchet MS" 
                               FontWeight="Bold" 
                               FontSize="14"/>
                </StackPanel>
            </Button>
        </Grid>
    </Grid>
</UserControl>
